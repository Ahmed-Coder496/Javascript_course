"use_strict"

//Functions :

function func(num1, num2) {
    return 2 + 2
}
func()        // => It call's the function
console.log(func);        // => It gives function reference
console.log(func());        // => 4
